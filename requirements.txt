# Inference only (DualLAAT.from_pretrained + .predict)
numpy==1.26.4
torch==2.3.1
tqdm==4.66.5

# Training + evaluation
lightning==2.4.0          # pytorch-lightning under the `lightning` namespace
pytorch-lightning==2.5.0
transformers==4.47.1      # only used for `get_scheduler`
pandas==2.2.3
pyarrow==16.1.0           # feather I/O via pandas
PyYAML==6.0.1
scikit-learn==1.3.1       # sklearn.metrics

# Data preparation (only for prepare_mimic*.py and train_w2v.py)
gensim==4.3.2             # word2vec training
